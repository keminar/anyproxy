# mstsc 远程桌面图标模糊 

## 被控端（服务端）优化

开启 RDP 硬件 GPU 加速（Win10/11 专业版）：

1. `Win+R` 输入 `gpedit.msc`（家庭版无组策略，可改注册表）
2. 计算机配置 → 管理模板 → Windows 组件 → 远程桌面服务 → 远程桌面会话主机 → 远程会话环境
3. 将"硬件图形适配器应用于所有远程桌面会话" → 已启用

重启远程被控电脑后，重新连接测试。
